<%= form_for(current_user.active_relationships.find_by(coachee_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Uncoach", class: "btn" %>
<% end %>